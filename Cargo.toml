[package]
name = "boot-dash"
version = "0.1.0"
edition = "2021"

[dependencies]
ratatui = "0.29"
crossterm = "0.28"
clap = { version = "4", features = ["derive"] }
chrono = "0.4"

[profile.release]
opt-level = 2
strip = true
